; Resources: bracket scopes a resource to one test; fixture shares one
; across the run and the runner releases it at the end.

(test
 (name test_storage)
 (modules test_storage db server)
 (libraries windtrap))
