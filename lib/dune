; windtrap_coverage is the coverage seam's data source: the runner
; snapshots in-process coverage at run end for the inline summary. The
; dependency points downward only — windtrap_coverage never links
; windtrap.

(library
 (name windtrap)
 (public_name windtrap)
 (libraries unix clock windtrap_coverage)
 (foreign_stubs
  (language c)
  (names capture_stubs)))
